# 복사: cp .env.example .env
# dev: localhost + Docker 포트 / prod: 별도 .env.prod 참고 (localhost 금지)

NODE_ENV=dev
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5555/artinerary
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=artinerary
POSTGRES_PORT=5555