# Railway — Nest API (루트). Next.js(web)는 Vercel에 배포.
# Dashboard: 서비스 Root Directory = / (저장소 루트), 이름 예: artinerary-api

[build]
buildCommand = "pnpm build"

[deploy]
startCommand = "pnpm start:prod"
restartPolicyType = "ON_FAILURE"
